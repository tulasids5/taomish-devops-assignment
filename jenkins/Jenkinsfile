pipeline {
    agent any

    environment {
        DOCKER_CREDS = credentials('dockerhub-creds')
        IMAGE_BACKEND = "${DOCKER_CREDS_USR}/backend-app"
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/tulasids5/taomish-devops-assignment.git'
            }
        }

        stage('Build Backend') {
            steps {
                sh 'mvn -f app/backend/pom.xml clean package -DskipTests'
            }
        }

        stage('Docker Build') {
            steps {
                sh "docker build -t ${IMAGE_BACKEND}:v1 -f docker/Dockerfile.backend app/backend"
            }
        }

        stage('Docker Login & Push') {
            steps {
                sh """
                echo ${DOCKER_CREDS_PSW} | docker login -u ${DOCKER_CREDS_USR} --password-stdin
                docker push ${IMAGE_BACKEND}:v1
                """
            }
        }
    }

    post {
        success {
            echo "SUCCESS: Backend CI/CD Completed "
        }
        failure {
            echo "FAILED "
        }
    }
}
